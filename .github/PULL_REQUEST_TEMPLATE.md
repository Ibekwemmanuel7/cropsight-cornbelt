## What this PR changes

<!-- One paragraph. What and why, not how. -->

## Linked issue or spec

<!-- e.g. closes #12, implements docs/specs/in_season_pipeline.md §2C -->

## Checklist

- [ ] Tests added or updated (see `tests/`)
- [ ] Leakage audit unaffected, or new check added if the change could see future data
- [ ] `python -m ruff check .` is clean locally
- [ ] `python -m ruff format --check .` is clean locally
- [ ] `python -m pytest` passes locally
- [ ] CHANGELOG.md updated under `[Unreleased]` (if user-facing)
- [ ] README updated if the install / run flow changed

## Before / after numbers

<!-- If this changes accuracy, intervals, or runtime, paste the before/after
     leaderboard or benchmark rows here. -->

## Anything reviewers should pay extra attention to

<!-- Subtle invariants, breaking changes, follow-up work. -->
